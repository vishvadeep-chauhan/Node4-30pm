<!-- 1  -  : The E-commerce Route (Basic Practice)
Create a new file called server2.js and set up an Express app.

The Goal: Create a route at /api/laptop.

The Output: When a user visits this route, use res.json() to send back an object representing a laptop.

Requirements: The object must include:

brand (String, e.g., "Apple" or "Dell")

model (String)

price (Number)

inStock (Boolean)

specs (Array of strings, e.g., ["16GB RAM", "512GB SSD"]) -->

<!-- task 2 -->
<!-- Create a route for a fitness application where a user sends their weight (in kg) and height (in meters) through the URL. The server should calculate their Body Mass Index (BMI) and return their fitness category. -->




  

Tech blogs and content hubs often need clean URLs. Create a route that takes an articleTitle and an authorName as parameters, and converts them into a clean, SEO-friendly slug (lowercase, spaces replaced with hyphens). -->

<!-- task4: Coding Bootcamp - Course Progress Tracker 

 Create a route for a coding bootcamp where the URL takes a studentId, totalModules, and completedModules. The backend must calculate the percentage of the course completed and send a motivational message. -->

Task 5: Fitness Center - Gym Membership Validator
Scenario: For a dynamic gym website, the backend needs to verify if a user's chosen subscription plan is valid and calculate the total cost. The route takes the planType (e.g., monthly, quarterly, yearly) and the months they want to enroll for.

Task 6: Traffic Authority - E-Challan Penalty Calculator
Scenario: Build a route for a traffic police system. The URL will accept a vehicleType and a violation. The server uses a switch statement to determine the penalty amount.

Task 7: Tech Influencer - Social Media Engagement Rate
Scenario: Content creators often need to calculate their engagement rate. Create a route that accepts total followers, likes, and comments as parameters. The server calculates the engagement percentage based on those interactions.
